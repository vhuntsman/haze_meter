# haze_meter
Example code for IoT HTTP client Haze Reader using mbed applications board.

Build instructions
1) Pre-requisite toolchain: https://launchpad.net/gcc-arm-embedded
2) In root directory, call make.
